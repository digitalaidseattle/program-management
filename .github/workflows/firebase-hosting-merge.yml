# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge
on:
  push:
    branches:
      - main
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    environment: production
    steps:
      - uses: actions/checkout@v4
      - run: |
          touch .env
          echo VITE_AIRTABLE_PM_ANON_KEY=${{ secrets.PM_ANON_KEY }} >> .env
          echo VITE_AIRTABLE_ANON_KEY=${{ secrets.DAS_ANON_KEY }} >> .env
          echo VITE_SUPABASE_ANON_KEY=${{ secrets.SUPBASE_ANON_KEY }} >> .env
          echo VITE_AIRTABLE_PAT=${{ secrets.AIRTABLE_PAT }} >> .env
          echo VITE_AIRTABLE_APPLICANT_PAT=${{ secrets.AIRTABLE_APPLICANT_PAT }} >> .env
          echo VITE_CALENDLY_CLIENT_ID=${{ secrets.CALENDLY_CLIENT_ID }} >> .env
          echo VITE_CALENDLY_CLIENT_SECRET=${{ secrets.CALENDLY_CLIENT_SECRET }} >> .env
          echo VITE_CODA_API_TOKEN=${{ secrets.CODA_API_TOKEN }} >> .env
          cat .env
          npm ci && npm run build
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: ${{ secrets.GITHUB_TOKEN }}
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_PROGRAM_MANAGEMENT_727E5 }}
          channelId: live
          projectId: program-management-727e5
